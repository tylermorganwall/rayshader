% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ggplot_extent.R
\name{get_ggplot_extent}
\alias{get_ggplot_extent}
\title{Get the Extent for a 3D ggplot Scene}
\usage{
get_ggplot_extent(heightmap = NULL, panel = NULL)
}
\arguments{
\item{heightmap}{Default \code{NULL}. A height matrix previously returned by \code{\link[=plot_gg]{plot_gg()}} with
\code{save_height_matrix = TRUE}. If supplied, the extent metadata will be read from the matrix
instead of the active 3D scene.}

\item{panel}{Default \code{NULL}. Panel index to return for faceted ggplots. If left \code{NULL} and the
scene contains a single panel, a single extent vector is returned. If multiple panels are present,
a named list of extents is returned.}
}
\value{
A named numeric vector with \code{xmin}, \code{xmax}, \code{ymin}, and \code{ymax}, or a named list of those
vectors for multi-panel plots.
}
\description{
Returns the synthetic extent generated by the most recent \code{\link[=plot_gg]{plot_gg()}} call.
This extent maps the plotted x/y data range into the full rasterized ggplot scene, which
allows post-hoc overlays to be added with functions such as \code{\link[=render_points]{render_points()}} or \code{\link[=render_path]{render_path()}}.
}
\examples{
if(run_documentation()) {
library(ggplot2)

mtplot = ggplot(mtcars) +
 geom_point(aes(x = wt, y = mpg, color = mpg))

plot_gg(mtplot, width = 4, windowsize = c(1000, 800))
gg_extent = get_ggplot_extent()

render_points(
 long = mtcars$wt,
 lat = mtcars$mpg,
 altitude = 20,
 color = "white",
 size = 4,
 extent = gg_extent
)
render_snapshot()
}
}
