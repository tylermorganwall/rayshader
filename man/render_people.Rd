% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_people.R
\name{render_people}
\alias{render_people}
\title{Render People}
\usage{
render_people(
  location = NULL,
  pose = "standing",
  sex = "male",
  line = NULL,
  line_spacing = 2,
  line_terrain_spacing = TRUE,
  line_pattern = NULL,
  line_align_terrain = TRUE,
  color = "white",
  angle = c(0, 0, 0),
  lit = TRUE,
  load_normals = TRUE,
  clear_previous = FALSE,
  x = NULL,
  y = NULL,
  altitude = NULL,
  xyz = NULL,
  offset = 0,
  lat = NULL,
  long = NULL,
  crs = NULL,
  filter_to_extent = TRUE,
  extent = NULL,
  panel = NULL,
  zscale = 1,
  vertical_exaggeration = 1,
  heightmap = NULL,
  ...
)
}
\arguments{
\item{location}{Default \code{NULL}. Spatial input used to place people in the
scene. Accepts \code{sf}, \code{sfc}, \code{sfg}, or \code{sp} POINT, MULTIPOINT, LINESTRING, or
MULTILINESTRING geometries. Point inputs place one model at every point. Line
inputs generate placements according to \code{line_spacing}. If the input carries a
CRS, it is transformed automatically into the active scene CRS. If it has no
CRS, supply \code{crs}.}

\item{pose}{Default \code{"standing"}. Bundled pose to render. Supply one value
for every person or a single value to use for all people. Options are
\code{"clapping"}, \code{"ironman"}, \code{"slipping"}, \code{"stack"}, \code{"standing"}, \code{"stop"},
\code{"stop_one_hand"}, \code{"stretch"}, \code{"walking"}, \code{"rocky"}, and \code{"yelling"}.}

\item{sex}{Default \code{"male"}. Person model variant. Options are \code{"male"}
and \code{"female"}.}

\item{line}{Default \code{NULL}. Spatial line input along which to place people.
Accepts \code{sf}, \code{sfc}, \code{sfg}, or \code{sp} LINESTRING or MULTILINESTRING geometry.
Line geometry may alternatively be supplied through \code{location}.}

\item{line_spacing}{Default \code{2}. Distance in meters between people placed along a
spatial line. For CRS-less lines, this is interpreted in the line's coordinate
units. Sampling starts at the beginning of every line component.}

\item{line_terrain_spacing}{Default \code{TRUE}. If \code{TRUE}, line spacing is measured
along the rendered terrain surface using the heightmap and effective \code{zscale}.
This keeps neighboring models together on slopes. If terrain metadata is not
available, spacing falls back to plan-view distance. Set to \code{FALSE} to always
use plan-view distance.}

\item{line_pattern}{Default \code{NULL}. Repeating male/female line_pattern used for line
placement. Supply a single string containing \code{M} and \code{F}, such as \code{"MF"} or
\code{"FMF"}. Overrides \code{sex} for line placements.}

\item{line_align_terrain}{Default \code{TRUE}. If \code{TRUE}, line placements are tilted
so each person's local +Y up vector matches the terrain normal. The local +Z
forward vector is lifted along the terrain without changing its plan-view
alignment with the line direction.}

\item{color}{Default \code{"white"}. Color of the person model. For line
placement, supply one color per generated person or one color per entry in
\code{line_pattern}; pattern colors are repeated with the pattern. Use \code{"height"} to
color placed models by the cached \code{\link[=plot_gg]{plot_gg()}} height aesthetic palette.}

\item{angle}{Default \code{c(0, 0, 0)}. Rotation around the x, y, and z axes. A
matrix or list can specify a separate rotation for each person. For line
placement, these rotations are added to the automatic line orientation.}

\item{lit}{Default \code{TRUE}. Whether to light the model polygons.}

\item{load_normals}{Default \code{TRUE}. Whether to load normals from the model.}

\item{clear_previous}{Default \code{FALSE}. If \code{TRUE}, remove previously rendered
people before rendering the new ones. A clear-only call returns without
rendering a replacement.}

\item{x}{Default \code{NULL}. Vector of x coordinates (or coordinates in the same
coordinate reference system as \code{extent}).}

\item{y}{Default \code{NULL}. Vector of y coordinates (or coordinates in the same
coordinate reference system as \code{extent}).}

\item{altitude}{Default \code{NULL}. Elevation of each person, in units of the
elevation matrix. If left \code{NULL}, each person is placed on the surface plus
\code{offset}. A single value places every person at that altitude. When one
horizontal location is supplied with multiple altitude values, that location
is repeated to place one person at each altitude.}

\item{xyz}{Default \code{NULL}. A three-column numeric matrix in which each row
specifies the raw rgl x/y/z coordinates of a person. Overrides coordinate
placement and \code{altitude}.}

\item{offset}{Default \code{0}. Offset from the surface when \code{altitude = NULL}.}

\item{lat}{Default \code{NULL}. Alias for \code{y} for geographic workflows.}

\item{long}{Default \code{NULL}. Alias for \code{x} for geographic workflows.}

\item{crs}{Default \code{NULL}. CRS of numeric x/y coordinates, or a CRS to assign
to CRS-less spatial data before transforming it into the active scene CRS.}

\item{filter_to_extent}{Default \code{TRUE}. If \code{TRUE}, placements outside the
scene extent are omitted.}

\item{extent}{Default \code{NULL}. Either an object representing the spatial
extent of the scene (from the \code{raster}, \code{terra}, \code{sf}, or \code{sp} packages), a
length-4 numeric vector specifying \code{c("xmin", "xmax", "ymin", "ymax")}, or a
spatial object that can be converted to an extent. If omitted, rayshader uses
extent metadata cached by \code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}}.}

\item{panel}{Default \code{NULL}. Facet panel identifier for scenes created with
\code{\link[=plot_gg]{plot_gg()}}. Required to disambiguate faceted ggplot scenes when
panel-specific cached metadata is needed. Ignored for non-ggplot scenes.}

\item{zscale}{Default \code{1}. Ratio between horizontal spacing and the elevation
units in the original heightmap. If omitted, rayshader uses the cached scene
value when available.}

\item{vertical_exaggeration}{Default \code{1}. Multiplier applied to the effective
visual relief. If omitted, rayshader uses the cached scene value from
\code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}} when available; pass explicitly to override it for
this call.}

\item{heightmap}{Default \code{NULL}. Height matrix for the current scene. If
omitted, rayshader uses the cached scene heightmap from \code{\link[=plot_3d]{plot_3d()}} or
\code{\link[=plot_gg]{plot_gg()}}. Terrain elevations and existing \code{NA} cells are read directly
from this matrix.}

\item{...}{Additional arguments passed to \code{\link[=render_obj]{render_obj()}} and then to
\code{rgl::triangles3d()}.}
}
\description{
Adds one or more 3D people to an existing rayshader scene. The
bundled person models use meter-based coordinates and are scaled using the
scene's effective \code{zscale}, including \code{vertical_exaggeration}, so that their
native two-meter stature remains correctly sized relative to the terrain.
Choose from several bundled poses with \code{pose} and select the model variant
with \code{sex}. The bundled Wavefront OBJ models use \code{.txt} file extensions
for R package compatibility.

When \code{line} is supplied, or \code{location} contains line geometry, people are
placed along each line component at regular intervals. Their local +Z axes
follow the direction of the line. Use \code{line_pattern} to repeat male and female
variants along the sampled positions.

Cache fallback messages are disabled by default. Set
\code{options(rayshader.verbose_scene_cache = TRUE)} to print when cached metadata
is reused.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
# Example 1
# Build a stepped pose gallery.
flat_heightmap = matrix(1:16 / 2, nrow = 4, byrow = TRUE) |>
  rayimage::render_reorient(flipy = TRUE, transpose = TRUE) |>
  rayimage::render_resized(mag = 50, method = "box")
scene_zscale = 1 / 25

flat_heightmap |>
  constant_shade("#800") |>
  plot_3d(
    heightmap = flat_heightmap,
    zscale = scene_zscale,
    vertical_exaggeration = 1,
    shadowdepth = 0,
    soliddepth = 0,
    solidcolor = "#800",
    windowsize = c(800, 800)
  )
render_camera(
  theta = -28,
  phi = 16,
  zoom = 0.70,
  fov = 0,
  shift_vertical = -10
)

# Place every pose on the podium. The ironman model is raised to
# clear its step, and the tenth person will support a second stack model.
# These x/y values use the raw matrix's cached 1-based extent.
gallery_positions = expand.grid(
  x = seq(25, 175, by = 50),
  y = seq(25, 125, by = 50)
)
gallery_poses = c(
  "standing",
  "walking",
  "stop",
  "stop_one_hand",
  "clapping",
  "yelling",
  "stretch",
  "slipping",
  "stack",
  "stack",
  "ironman",
  "rocky"
)
gallery_positions$offset = 0
gallery_positions$offset[gallery_poses == "ironman"] = 1
stack_index = 10L
stack_position = gallery_positions[stack_index, , drop = FALSE]
person_height = 2
label_clearance = 0.1

# Render and label male pose gallery
render_people(
  pose = gallery_poses,
  sex = "male",
  clear_previous = TRUE,
  x = gallery_positions$x,
  y = gallery_positions$y,
  offset = gallery_positions$offset
)
render_people(
  pose = "stack",
  sex = "male",
  x = stack_position$x,
  y = stack_position$y,
  offset = stack_position$offset + person_height
)

# Label the standalone models first, then label the completed tower above
# its upper person without relying on negative indexing.
regular_label_indices = setdiff(seq_along(gallery_poses), stack_index)
render_label(
  text = gallery_poses[regular_label_indices],
  font = "Helvetica",
  family = "serif",
  fonttype = "standard",
  textcolor = "white",
  line = FALSE,
  clear_previous = TRUE,
  x = gallery_positions$x[regular_label_indices],
  y = gallery_positions$y[regular_label_indices],
  altitude = gallery_positions$offset[regular_label_indices] +
    person_height + label_clearance,
  relativez = TRUE
)
render_label(
  text = "stack",
  font = "Helvetica",
  family = "serif",
  fonttype = "standard",
  textcolor = "white",
  line = FALSE,
  x = stack_position$x,
  y = stack_position$y,
  altitude = stack_position$offset + 2 * person_height + label_clearance,
  relativez = TRUE
)

# Produce an 800-by-800 high-quality image of the male pose variants.
render_highquality(
  width = 800,
  height = 800,
  sky_sun_elevation = 10,
  sky_sun_azimuth = 160,
  iso = 5,
  samples=16
)

# Female models
render_people(
  pose = gallery_poses,
  sex = "female",
  clear_previous = TRUE,
  x = gallery_positions$x,
  y = gallery_positions$y,
  offset = gallery_positions$offset
)
render_people(
  pose = "stack",
  sex = "female",
  x = stack_position$x,
  y = stack_position$y,
  offset = stack_position$offset + person_height
)
render_highquality(
  width = 800,
  height = 800,
  sky_sun_elevation = 10,
  sky_sun_azimuth = 160,
  iso = 5,
  samples=16
)

# Example 2
# Show the scale of the "volcano" 3D dataset
volcano_dem = volcano_spatial()
volcano_dem |>
  height_shade() |>
  plot_3d(
    windowsize = c(800, 800)
  )

render_trails(
  volcano_trails,
  color = "grey50",
  width = 2,
  preview = "mesh",
  width_units = "meters"
)

summit_walk = volcano_trails[
  !is.na(volcano_trails$name) &
    volcano_trails$name == "Puhi Huia Road",
]
render_snapshot()
render_people(
  line = summit_walk,
  pose = "walking",
  line_spacing = 5,
  offset=0.2,
  line_pattern = "MF",
  color = c("#F6E8C3", "white"),
  clear_previous = TRUE
)
render_camera(theta = -36, phi = 15, zoom = 0.08, fov=100)
render_snapshot()

# Example 3
# Arrange alternating models around a closed line. Models orient themselves
# according to the local normal and orient the people along the line
bump_heightmap = rayimage::generate_2d_gaussian(dim = c(30,30)) * 1000
bump_heightmap |>
  height_shade() |>
  plot_3d()

circle_r = function(x) {
  theta = seq(0, 2 * pi, length.out = 200)
  sf::st_linestring(cbind(
    15.5 + x * cos(theta),
    15.5 + x * sin(theta)
  ))
}
people_line = sf::st_sfc(
  circle_r(12.5),
  circle_r(10.5),
  circle_r(8.5),
  circle_r(6.5),
  circle_r(4.5),
  circle_r(2.5)
)
render_people(
  line = people_line,
  pose = "stretch",
  line_pattern = "MF",
  line_spacing = 2,
  color = c("dodgerblue", "tomato"),
)
render_people(
  x = c(13.5, 15.5, 17.5),
  y = rep(15.5, 3),
  offset = c(2,0,2),
  angle = list(c(30,45,0),c(0,0,0),c(30,-45,0)),
  pose = c("ironman", "rocky", "ironman"),
  color = "white"
)
render_camera(location = c(15.5,15.5), theta = 0, phi = 10,zoom=0.2,fov=80)
render_highquality(lightdirection = 225,samples=16)
\dontshow{\}) # examplesIf}
}
