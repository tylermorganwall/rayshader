% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_roads.R
\name{plot_render_road_topology}
\alias{plot_render_road_topology}
\title{Plot Road Layer Topology}
\usage{
plot_render_road_topology(
  roads,
  layer,
  layer_height = 5.5,
  boundary = NULL,
  boundary_tolerance = 1,
  endpoint_tolerance = 0.01,
  continuation_tolerance = 0.25,
  direction_lookahead = 8,
  continuation_ambiguity_margin = 0.04,
  continuation_stub_length = 8,
  views = c("overview", "crossing_detail"),
  focus = NULL,
  labels = TRUE,
  max_labels = 40,
  label_components = TRUE,
  label_event_order = TRUE,
  label_continuation_pairs = TRUE,
  layer_colors = NULL,
  main = NULL,
  filename = NULL,
  width = 2400,
  height = 1400,
  res = 150
)
}
\arguments{
\item{roads}{Road \code{sf} LINESTRING or MULTILINESTRING features.}

\item{layer}{An unquoted or character column name containing
OpenStreetMap-style layer values. Missing values are treated as implicit
layer \code{0} while retaining their implicit status.}

\item{layer_height}{Default \code{5.5}. A positive constant clearance or an
unquoted or character column name containing positive feature clearances.}

\item{boundary}{Default \code{NULL}. Optional supplied-data boundary geometry.
Endpoints near this boundary are marked in the returned diagnostics.}

\item{boundary_tolerance}{Default \code{1}. Distance in metres used to identify
endpoints on the supplied-data boundary.}

\item{endpoint_tolerance}{Default \code{1e-2}. Exact topology tolerance in metres.}

\item{continuation_tolerance}{Default \code{0.25}. Maximum true continuation gap
in metres.}

\item{direction_lookahead}{Default \code{8}. Physical distance in metres used to
estimate inward endpoint tangents.}

\item{continuation_ambiguity_margin}{Default \code{0.04}. Direction-score margin
used to identify near-ties within the best continuation evidence tier.}

\item{continuation_stub_length}{Default \code{8}. Physical inward-stub length in
metres used to draw pair-specific continuation diagnostics.}

\item{views}{Default \code{c("overview", "crossing_detail")}. Panels to draw.
Supported values are \code{"overview"} and \code{"crossing_detail"}.}

\item{focus}{Default \code{NULL}. Optional two-value metric center or four-value
metric bounding box for the crossing-detail panel. By default, the function
locates the densest crossing neighborhood.}

\item{labels}{Default \code{TRUE}. Whether to label source, fragment, layer, and
component IDs.}

\item{max_labels}{Default \code{40}. Maximum number of fragment labels per panel.}

\item{label_components}{Default \code{TRUE}. Whether to draw component IDs.}

\item{label_event_order}{Default \code{TRUE}. Whether the detail panel labels each
crossing participant with its layer and dense local rank.}

\item{label_continuation_pairs}{Default \code{TRUE}. Whether to label selected and
ambiguous continuation fragment pairs.}

\item{layer_colors}{Default \code{NULL}. Optional named or layer-ordered colors.}

\item{main}{Default \code{NULL}. Optional title or one title per requested view.}

\item{filename}{Default \code{NULL}. Optional PNG output path. When omitted, the
diagnostic is drawn on the active graphics device.}

\item{width}{Default \code{2400}. PNG width in pixels.}

\item{height}{Default \code{1400}. PNG height in pixels.}

\item{res}{Default \code{150}. PNG resolution in pixels per inch.}
}
\value{
Invisibly returns a \code{render_road_topology} list containing prepared
fragments, exact events, continuation diagnostics, network and prospective
solve \code{igraph} objects, component membership, and plot metadata.
}
\description{
Processes road features into the local layer-event topology
used by \code{\link[=render_roads]{render_roads()}} and draws reproducible two-dimensional diagnostics.
Point pairs are grouped into unified physical events before the plot
distinguishes junction-equality candidates, interior crossings,
topology/layer conflicts, overlaps, selected and ambiguous continuations,
physical network components, and the smaller prospective profile-solve
graph.
}
