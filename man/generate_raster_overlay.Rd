% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_raster_overlay.R
\name{generate_raster_overlay}
\alias{generate_raster_overlay}
\title{Generate Raster Overlay}
\usage{
generate_raster_overlay(
  raster,
  extent = NULL,
  heightmap = NULL,
  width = NA,
  height = NA,
  resolution_multiply = 1,
  palette = "red",
  alpha = 0.5,
  range = NULL,
  resample_method = "bilinear"
)
}
\arguments{
\item{raster}{A raster layer. This can be a \code{terra::SpatRaster}, a
\code{raster::RasterLayer}/\code{RasterBrick}/\code{RasterStack}, or a raster filename.}

\item{extent}{Default \code{NULL}. Either an object representing the spatial extent of the scene
(either from the \code{raster}, \code{terra}, \code{sf}, or \code{sp} packages),
a length-4 numeric vector specifying \code{c("xmin", "xmax","ymin","ymax")}, or the spatial object (from
the previously aforementioned packages) which will be automatically converted to an extent object.
If omitted, rayshader will infer the extent from \code{heightmap} when possible,
otherwise reuse cached extent metadata from the active scene or the most recent
raster-backed hillshade call.}

\item{heightmap}{Default \code{NULL}. The original height map. Pass this in to extract the dimensions of the resulting
overlay automatically. If omitted, rayshader will reuse the cached heightmap
from the active scene or the most recent hillshade call.}

\item{width}{Default \code{NA}. Width of the resulting overlay. Default the same dimensions as height map.}

\item{height}{Default \code{NA}. Height of the resulting overlay. Default the same dimensions as height map.}

\item{resolution_multiply}{Default \code{1}. If passing in \code{heightmap} instead of width/height, amount to
increase the resolution of the overlay.
Should be combined with \code{\link[=add_overlay]{add_overlay()}} with \code{rescale_original = TRUE} to ensure those added details are captured
in the final map.}

\item{palette}{Default \code{"red"}. Single color, vector of colors, or palette function.
A single color draws every finite raster cell with that color. Multiple colors
or a palette function map finite raster values continuously from low to high.}

\item{alpha}{Default \code{0.5}. Alpha multiplier for finite raster cells. Must be between \code{0} and \code{1}.
Alpha values included in \code{palette} colors are multiplied by this value.}

\item{range}{Default \code{NULL}. Numeric length-2 range used to map raster values
through \code{palette}. Values outside this range are clamped. If \code{NULL}, the
finite value range of the aligned raster is used.}

\item{resample_method}{Default \code{"bilinear"}. Method passed to
\code{terra::resample()} or \code{terra::project()} when aligning \code{raster} to the
scene grid.}
}
\value{
4-layer RGBA array representing the raster overlay.
}
\description{
Converts a spatial raster into a semi-transparent RGBA overlay
for the current rayshader scene. Raster values can be drawn with a single
color or mapped through a color palette.
}
\examples{
if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) {
  # Create a semi-transparent overlay from low elevations:
  low_elevation = montereybay_spatial
  low_elevation[low_elevation > 0] = NA

  montereybay_spatial |>
    height_shade() |>
    add_overlay(generate_raster_overlay(low_elevation, palette = "dodgerblue3")) |>
    plot_map()
}
}
