% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indent_surface.R
\name{indent_surface}
\alias{indent_surface}
\title{Indent or Raise a Heightmap Surface}
\usage{
indent_surface(
  heightmap,
  geometry,
  amount = 1,
  direction = c("down", "up"),
  transition = 0,
  extent = NULL,
  crs = NULL,
  touches = TRUE,
  fun = "max"
)
}
\arguments{
\item{heightmap}{Height matrix or spatial raster DEM. Matrix inputs require
\code{extent} or cached/attribute extent metadata. Spatial raster inputs return a
spatial raster; matrix inputs return a matrix.}

\item{geometry}{Spatial geometry used to select the affected cells. Supports
\code{sf}, \code{sfc}, \code{sfg}, \code{terra::SpatVector}, and \code{sp} vector objects.}

\item{amount}{Default \code{1}. Constant amount to apply, numeric vector with one
value per feature, or a single character string naming a numeric column in
\code{geometry}.}

\item{direction}{Default \code{"down"}. Direction to apply \code{amount}. \code{"down"}
subtracts from \code{heightmap}; \code{"up"} adds to \code{heightmap}.}

\item{transition}{Default \code{0}. Distance in map units over which the
indentation amount transitions from the polygon edge to the full amount.
\code{0} applies the full amount abruptly to all selected cells.}

\item{extent}{Default \code{NULL}. Spatial extent for matrix \code{heightmap} inputs.
Ignored for spatial raster inputs.}

\item{crs}{Default \code{NULL}. CRS for matrix \code{heightmap} inputs or CRS-less
spatial raster inputs. If a spatial raster already has a CRS, explicit \code{crs}
must match it.}

\item{touches}{Default \code{TRUE}. Passed to \code{terra::rasterize()}. If \code{TRUE},
cells touched by polygons or lines are modified.}

\item{fun}{Default \code{"max"}. Rasterization reducer passed to
\code{terra::rasterize()} for cells intersected by multiple features.}
}
\value{
A modified height matrix or \code{terra::SpatRaster}. Legacy \code{Raster*}
inputs are returned as \code{terra::SpatRaster} objects.
}
\description{
Modifies a heightmap under spatial geometries by a constant or per-feature
amount. This is useful when a terrain DEM lacks matching bathymetry and a
water surface would otherwise be coincident with the terrain surface.

Positive \code{amount} values lower the surface with \code{direction = "down"} and
raise it with \code{direction = "up"}. Negative values are allowed and reverse the
effect.
}
\examples{
\dontshow{if (length(find.package("sf", quiet = TRUE)) > 0) withAutoprint(\{ # examplesIf}
water_poly = sf::st_sfc(
  sf::st_polygon(list(rbind(
    c(0.2, 0.2), c(0.8, 0.2), c(0.8, 0.8), c(0.2, 0.8), c(0.2, 0.2)
  )))
)
lowered = indent_surface(
  matrix(10, 10, 10),
  water_poly,
  amount = 2,
  extent = c(0, 1, 0, 1)
)
\dontshow{\}) # examplesIf}
}
