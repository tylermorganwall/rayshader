% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ggplot_extent.R
\name{transform_ggplot_sf}
\alias{transform_ggplot_sf}
\title{Transform \code{sf} Geometries via ggplot2 Scales and Coord System}
\usage{
transform_ggplot_sf(
  sf_object,
  panel = NULL,
  heightmap = NULL,
  crs = NULL,
  segmentize_df_max_length = NULL
)
}
\arguments{
\item{sf_object}{An \code{sf}, \code{sfc}, or \code{sfg} object containing 2D geometry.}

\item{panel}{Default \code{NULL}. Facet panel index to use. Required if the scene has
multiple panels.}

\item{heightmap}{Default \code{NULL}. Height matrix returned by \code{\link[=plot_gg]{plot_gg()}} with
\code{save_height_matrix = TRUE}. If supplied, transformation metadata and extent are read
from this matrix instead of the active scene.}

\item{crs}{Default \code{NULL}. Only used for \code{coord_sf()}. Coordinate reference system for
input geometry if you want to override or define missing CRS metadata.}

\item{segmentize_df_max_length}{Default \code{NULL}. If provided, calls \code{\link[sf:geos_unary]{sf::st_segmentize()}}
before transformation to densify geometry edges. Units follow \code{sf} semantics.}
}
\value{
Geometry transformed into the same coordinate space as \code{attr(result, "extent")}.
For \code{sf} input, returns \code{sf}; for \code{sfc} input, returns \code{sfc}; for \code{sfg} input, returns
an \code{sfg}. The returned object includes \code{attr(result, "extent")} and \code{attr(result, "panel")}.
}
\description{
Transforms \code{sf} geometries through the same ggplot2 scale and coordinate
system used by the most recent \code{\link[=plot_gg]{plot_gg()}} scene.
}
\examples{
if(run_documentation()) {
library(ggplot2)
library(sf)

p = ggplot(spData::world) +
 geom_sf() +
 coord_sf(crs = sf::st_crs("+proj=robin"))

plot_gg(p, width = 6)
countries = spData::world[spData::world$name_long \%in\% c("France", "Spain"), ]
countries_t = transform_ggplot_sf(countries)

render_polygons(
 countries_t,
 extent = attr(countries_t, "extent"),
 top = 20,
 color = "red"
)
render_snapshot()
}
}
