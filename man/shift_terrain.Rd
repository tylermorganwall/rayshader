% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indent_surface.R
\name{shift_terrain}
\alias{shift_terrain}
\title{Shift a Heightmap Surface}
\usage{
shift_terrain(
  heightmap,
  geometry,
  amount = 1,
  transition = 0,
  transition_units = c("map", "cells"),
  extent = NULL,
  crs = NULL,
  touches = TRUE,
  fun = "max"
)
}
\arguments{
\item{heightmap}{Height matrix or spatial raster DEM. Matrix inputs require
\code{extent} or cached or attribute extent metadata. Spatial raster inputs return
a spatial raster; matrix inputs return a matrix.}

\item{geometry}{Spatial geometry used to select the affected cells. Supports
\code{sf}, \code{sfc}, \code{sfg}, \code{terra::SpatVector}, and \code{sp} vector objects.}

\item{amount}{Default \code{1}. Constant amount to apply, numeric vector with one
value per feature, or a single character string naming a numeric column in
\code{geometry}. Positive values raise terrain, negative values lower terrain,
and \code{NA} values skip their features.}

\item{transition}{Default \code{0}. Distance over which \code{amount} transitions from
the polygon edge to the full shift. \code{0} applies the full amount abruptly to
all selected cells.}

\item{transition_units}{Default \code{c("map", "cells")}. Units for \code{transition}.
\code{"map"} uses meters for longitude and latitude rasters, the CRS linear units
for projected rasters, and coordinate units for CRS-less rasters. \code{"cells"}
measures distance in unit-width and unit-height raster cells, independent of
CRS and spatial resolution.}

\item{extent}{Default \code{NULL}. Spatial extent for matrix \code{heightmap} inputs.
Ignored for spatial raster inputs.}

\item{crs}{Default \code{NULL}. CRS for matrix \code{heightmap} inputs or CRS-less
spatial raster inputs. If a spatial raster already has a CRS, explicit \code{crs}
must match it.}

\item{touches}{Default \code{TRUE}. Passed to \code{terra::rasterize()}. If \code{TRUE},
cells touched by polygons or lines are modified.}

\item{fun}{Default \code{"max"}. Rasterization reducer passed to
\code{terra::rasterize()} for cells intersected by multiple features.}
}
\value{
A shifted height matrix or \code{terra::SpatRaster}. Legacy \verb{Raster*}
inputs are returned as \code{terra::SpatRaster} objects.
}
\description{
Shifts a heightmap under spatial geometries by a constant or per-feature
amount. This is useful when terrain needs to be lowered below water or raised
beneath structures.

Positive \code{amount} values raise the surface and negative values lower it.
}
\details{
In map mode, transition distance is measured by \code{terra::distance()}. The
center-to-boundary inset is one-half of the smaller raster resolution. In
cell mode, distance is calculated on a temporary unit-resolution raster and
uses a half-cell inset of \code{0.5}. A polygon that fills the raster has no
internal polygon edge, so the raster boundary does not reduce its shift.
}
\examples{
\dontshow{if (length(find.package("sf", quiet = TRUE)) > 0) withAutoprint(\{ # examplesIf}
water_poly = sf::st_sfc(
  sf::st_polygon(list(rbind(
    c(0.2, 0.2), c(0.8, 0.2), c(0.8, 0.8), c(0.2, 0.8), c(0.2, 0.2)
  ))),
  crs = 4326
)
lowered = shift_terrain(
  matrix(10, 10, 10),
  water_poly,
  amount = -2,
  extent = c(0, 1, 0, 1)
)
\dontshow{\}) # examplesIf}
}
