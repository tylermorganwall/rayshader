% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_trails.R
\name{render_trails}
\alias{render_trails}
\title{Render Trail Paths}
\usage{
render_trails(
  trails,
  color = "grey50",
  width = 1,
  width_column = NULL,
  width_units = c("scene", "meters"),
  densify = TRUE,
  offset = NULL,
  height = 0.05,
  clear_previous = TRUE,
  preview = c("line", "mesh"),
  verbose = FALSE,
  parallel = TRUE,
  zscale = 1,
  vertical_exaggeration = 1,
  heightmap = NULL
)
}
\arguments{
\item{trails}{Spatial line data used to draw trails. Supports \code{sf}, \code{sfc},
\code{sfg}, \code{SpatialLines}, and \code{SpatialLinesDataFrame} line inputs.}

\item{color}{Default \code{"grey50"}. Trail color.}

\item{width}{Default \code{1}. Trail width in the units selected by \code{width_units}.}

\item{width_column}{Default \code{NULL}. Column name in an \code{sf} trail object used
to set per-feature trail widths. Values must be positive finite numbers and
use the same units as \code{width}.}

\item{width_units}{Default \code{"scene"}. Units used by \code{width} and
\code{width_column}. \code{"scene"} uses scene grid-cell units; \code{"meters"} converts
physical widths using the scene extent and CRS.}

\item{densify}{Default \code{TRUE}. Whether to densify trails and resample them
along the terrain before meshing.}

\item{offset}{Default \code{NULL}. Vertical trail centerline offset in elevation
units. When \code{NULL}, the mesh is centered on the terrain surface.}

\item{height}{Default \code{0.05}. Total solid mesh thickness in scene units.}

\item{clear_previous}{Default \code{TRUE}. Whether to remove the existing trail
layer before drawing the new one. A clear-only call returns without drawing
a replacement.}

\item{preview}{Default \code{"line"}. Whether trails are drawn in rgl as
lightweight \code{"line"} previews or as their cached \code{"mesh"} geometry. Both
options use the solid mesh in \code{\link[=render_highquality]{render_highquality()}}.}

\item{verbose}{Default \code{FALSE}. Whether to show native meshing progress.}

\item{parallel}{Default \code{TRUE}. Whether to build independent trail meshes in
parallel with the native job queue.}

\item{zscale}{Default \code{1}. Ratio between horizontal spacing and elevation
units. If omitted for a spatial raster scene, rayshader uses its resolution.}

\item{vertical_exaggeration}{Default \code{1}. Multiplier applied to the effective
visual relief. If omitted, rayshader uses the active scene value.}

\item{heightmap}{Default \code{NULL}. Height matrix or spatial raster for the
current scene. If omitted, rayshader uses the active scene heightmap.}
}
\value{
Invisibly returns the independently meshed trail coordinates.
}
\description{
Adds trails that follow the terrain surface. Trails use
lightweight rgl line previews while caching solid meshes for
\code{\link[=render_highquality]{render_highquality()}}. Each input line is meshed independently, without
road grades, lanes, layer rules, intersection solving, or mesh joining.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
volcano_dem = volcano_spatial()
volcano_dem |>
  height_shade() |>
  plot_3d()

render_trails(
  volcano_trails,
  color = "grey50",
  width = 3,
  width_units = "meters"
)
render_camera(theta=-33,phi=40,zoom=0.4)
# RGL render uses lines for speed
render_snapshot()
# High quality render uses mesh trail
render_highquality(samples=16)
# RGL preview can be set to use (potentially slower) mesh preview
render_trails(
  volcano_trails,
  color = "grey50",
  width = 3,
  width_units = "meters",
  preview="mesh", 
  clear_previous = TRUE
)
render_snapshot()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=render_streams]{render_streams()}}, \code{\link[=render_roads]{render_roads()}}
}
