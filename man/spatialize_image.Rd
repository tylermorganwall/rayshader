% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatialize_image.R
\name{spatialize_image}
\alias{spatialize_image}
\title{Spatialize a Rayshader Image}
\usage{
spatialize_image(
  image,
  extent = NULL,
  crs = NULL,
  panel = NULL,
  include_alpha = FALSE,
  toRGB = TRUE,
  flip_vertical = FALSE,
  flip_horizontal = FALSE,
  layer_names = NULL,
  include_height = FALSE,
  heightmap = NULL,
  height_layer_name = "height"
)
}
\arguments{
\item{image}{A matrix, RGB/RGBA array, or any input accepted by
\code{rayimage::ray_read_image()}.}

\item{extent}{Default \code{NULL}. Spatial extent metadata for the output raster.
Explicit \code{extent} overrides cached metadata. Accepts the same extent-like
inputs supported by \code{\link[=get_extent]{get_extent()}}, including numeric
\code{c(xmin, xmax, ymin, ymax)} vectors, \code{terra::SpatExtent},
\code{terra::SpatRaster}, \code{raster::Extent}, \verb{Raster*}, \code{sf}/\code{sfc}, and \code{sp}
objects. If omitted, rayshader reuses cached scene extent metadata.}

\item{crs}{Default \code{NULL}. CRS for the output raster. Explicit \code{crs}
overrides everything else. If omitted, rayshader first tries to inherit CRS
from the explicit \code{extent} object, then from cached scene metadata.}

\item{panel}{Default \code{NULL}. Facet panel identifier for scenes created with
\code{\link[=plot_gg]{plot_gg()}}. Required to disambiguate faceted ggplot scenes when panel-
specific cached metadata is needed. Ignored for non-faceted/non-ggplot
scenes.}

\item{include_alpha}{Default \code{FALSE}. If \code{FALSE} and \code{image} has four
channels, drop alpha. If \code{TRUE}, include alpha as a fourth raster layer.}

\item{toRGB}{Default \code{TRUE}. If \code{TRUE}, convert color channels from linear
rayshader values to sRGB with \code{\link[rayimage:render_gamma_linear]{rayimage::render_gamma_linear()}}, then scale
to the \code{0-255} range and clamp. Alpha is not gamma-adjusted, but is scaled
and clamped to match.}

\item{flip_vertical}{Default \code{FALSE}. If \code{TRUE}, reverse the image row order
before rasterization.}

\item{flip_horizontal}{Default \code{FALSE}. If \code{TRUE}, reverse the image column
order before rasterization.}

\item{layer_names}{Default \code{NULL}. Optional output layer names. Defaults are
\code{"value"} for a single layer, \code{c("red", "green", "blue")} for RGB output,
and \code{c("red", "green", "blue", "alpha")} for RGBA output.}

\item{include_height}{Default \code{FALSE}. If \code{TRUE}, append a final layer with
raw height values from \code{heightmap} or cached rayshader scene metadata. When
the output image resolution differs from the source heightmap, heights are
bilinearly resampled onto the output raster grid.}

\item{heightmap}{Default \code{NULL}. Optional source elevation matrix or spatial
raster used when \code{include_height = TRUE}. Explicit \code{heightmap} overrides
cached heightmap metadata. If omitted, rayshader reuses cached hillshade or
scene heightmap metadata when available. Faceted cached \code{plot_gg()} scenes
require an explicit \code{heightmap} for height export.}

\item{height_layer_name}{Default \code{"height"}. Name of the appended height
layer when \code{include_height = TRUE}.}
}
\value{
A \code{terra::SpatRaster}.
}
\description{
\code{spatialize_image()} converts a rayshader image into a georeferenced
\code{terra::SpatRaster}. If they are omitted, rayshader reuses
cached extent and CRS metadata from the active scene, following the same
cache-resolution rules used by the \verb{render_*()} helpers. Cached metadata can
come from \code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}}, and \code{panel} can be used to select a
specific facet panel for faceted ggplot scenes.  Explicit \code{extent} and \code{crs}
arguments always take precedence.
}
\details{
If no explicit extent is supplied and no cached extent metadata is available,
\code{spatialize_image()} errors. CRS is resolved in this order: explicit \code{crs},
CRS carried by the explicit \code{extent} object, cached scene CRS, then empty CRS
if none is available.

Cache fallback messages are disabled by default. Set
\code{options(rayshader.verbose_scene_cache = TRUE)} to print when cached metadata
is reused.
}
\examples{
\dontshow{if (length(find.package("terra", quiet = TRUE)) > 0) withAutoprint(\{ # examplesIf}
hillshade_img = elmat |>
  sphere_shade() |>
  add_shadow(ray_shade(elmat), 0.5)

hillshade_rast = spatialize_image(
  hillshade_img,
  extent = raster::extent(c(0, 1, 0, 1)),
  crs = "EPSG:4326"
)
\dontshow{\}) # examplesIf}
}
