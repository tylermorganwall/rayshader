% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_scene_metadata.R
\name{get_scene_metadata}
\alias{get_scene_metadata}
\title{Get Cached Scene Metadata}
\usage{
get_scene_metadata(include_heightmap = FALSE)
}
\arguments{
\item{include_heightmap}{Default \code{FALSE}. Whether to include the cached
height matrix in the returned list. The heightmap can be large; its dimensions
and elevation range are always returned.}
}
\value{
A named list containing:
\itemize{
\item \code{scene_id}: The active rgl scene identifier.
\item \code{plot_type}: Either \code{"plot_3d"} or \code{"plot_gg"}.
\item \code{extent}: The cached x/y extent, or \code{NULL} when there is no single extent.
\item \code{extent_3d}: The x/y extent plus \code{zmin} and \code{zmax} elevation bounds.
\item \code{crs}: The scene coordinate reference system, when available.
\item \code{zscale}, \code{vertical_exaggeration}, and \code{effective_zscale}: The cached base
elevation scale, relief multiplier, and their effective ratio.
\item \code{heightmap_dimensions}, \code{elevation_range}, and \code{scene_elevation_range}:
Terrain matrix dimensions and vertical bounds in input and scene units.
\item \code{scene_bounds}, \code{scene_center}, and \code{scene_dimensions}: Terrain bounds,
center, and size in rgl x/y/z coordinates.
\item \code{geographic_aspect}: Cached horizontal aspect and physical cell metadata.
\item \code{meters_per_scene_unit} and \code{map_units_per_scene_unit}: Available physical
and coordinate-unit conversions for horizontal scene distances.
\item \code{scene_axis_mapping}: Mapping from spatial axes to rgl scene axes.
\item \code{triangulate}: Whether the terrain used mesh simplification.
\item \code{panel_extents} and \code{panel_info}: Cached \code{\link[=plot_gg]{plot_gg()}} panel metadata, when
applicable.
\item \code{sources}: Labels describing the expressions that populated core cache
values.
\item \code{heightmap}: The cached matrix when \code{include_heightmap = TRUE}, otherwise
\code{NULL}.
}
}
\description{
Returns the spatial and 3D coordinate metadata cached for the
active scene created by \code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}}. This provides the
information needed to transform user data or size procedural geometry to the
rendered terrain without reaching into rayshader's internal cache.

The returned \code{extent_3d} uses conventional spatial axes: x, y, and elevation
z in the cached heightmap's units. For \code{\link[=plot_3d]{plot_3d()}} these are the input
elevation units; \code{\link[=plot_gg]{plot_gg()}} may use a transformed height scale.
\code{scene_bounds} uses rgl axes: x, vertical y, and horizontal z. Spatial y
increases toward negative scene z, and cached heightmap elevations are divided
by \code{effective_zscale} to obtain scene y coordinates.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
volcano_spatial() |>
  height_shade() |>
  plot_3d()

scene_info = get_scene_metadata()
scene_info$extent_3d
scene_info$scene_bounds
\dontshow{\}) # examplesIf}
}
