% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_roads.R
\name{build_render_road_profile_problem}
\alias{build_render_road_profile_problem}
\title{Build a sparse quadratic road profile problem}
\usage{
build_render_road_profile_problem(
  topology,
  terrain_profiles = NULL,
  explicit_controls = NULL,
  layer_spacing = 5.5,
  maximum_grade = 0.07,
  maximum_grade_rate = 0.001,
  curvature_weight = 100,
  grade_weight = 1,
  terrain_reference_weight = 0.001,
  underground_reference_depth = NULL,
  underground_reference_weight = 0.001,
  anchor_grade_weight = 10,
  uplift_weight = 1e-05,
  anchor_grade_window = 10,
  control_tolerance = 1e-07,
  adaptive_constraints = NULL
)
}
\arguments{
\item{topology}{Road topology diagnostics.}

\item{terrain_profiles}{Default \code{NULL}. Terrain reference profiles accepted
by \code{\link[=normalize_render_road_terrain_profiles]{normalize_render_road_terrain_profiles()}}.}

\item{explicit_controls}{Default \code{NULL}. Optional list of additional control
distances by fragment.}

\item{layer_spacing}{Default \code{5.5}. Fallback adjacent-layer clearance in
metres.}

\item{maximum_grade}{Default \code{0.07}. Maximum absolute longitudinal grade.}

\item{maximum_grade_rate}{Default \code{1e-3}. Maximum grade change per metre.}

\item{curvature_weight}{Default \code{100}. Objective weight on grade change.}

\item{grade_weight}{Default \code{1}. Objective weight on grade magnitude.}

\item{terrain_reference_weight}{Default \code{1e-3}. Objective weight toward the
sampled terrain reference.}

\item{underground_reference_depth}{Default \code{NULL}, which uses
\code{layer_spacing}. Terrain-relative reference depth in metres for explicit
underground fragments.}

\item{underground_reference_weight}{Default \code{1e-3}. Positive objective
weight that bounds explicit underground profiles around their reference
depth.}

\item{anchor_grade_weight}{Default \code{10}. Objective weight toward terrain
grade at fixed outer anchors.}

\item{uplift_weight}{Default \code{1e-5}. Linear objective weight discouraging
unnecessary elevation.}

\item{anchor_grade_window}{Default \code{10}. Physical terrain-regression window
in metres at confirmed ground anchors.}

\item{control_tolerance}{Default \code{1e-7}. Maximum distance in metres when
resolving an event or endpoint to a compiled control.}

\item{adaptive_constraints}{Default \code{NULL}. Continuous-audit constraint
requests used when rebuilding a refined problem.}
}
\value{
Sparse matrices, controls, constraints, and solve diagnostics.
}
\description{
Build a sparse quadratic road profile problem
}
\keyword{internal}
