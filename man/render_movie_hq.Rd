% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_movie.R
\name{render_movie_hq}
\alias{render_movie_hq}
\title{Render High Quality Movie}
\usage{
render_movie_hq(
  frames = 360,
  type = "linear",
  closed = TRUE,
  damp_motion = TRUE,
  render_highquality_args = list(),
  ...
)
}
\arguments{
\item{frames}{Default \code{360}. Number of frames in the generated camera motion.}

\item{type}{Default \code{"linear"}. Type of transition between saved keyframes.
Other options are those supported by \code{rayrender::generate_camera_motion()}.}

\item{closed}{Default \code{TRUE}. Whether to close the camera path by returning to
the first saved keyframe.}

\item{damp_motion}{Default \code{TRUE}. Whether to damp quick camera movements.}

\item{render_highquality_args}{Default \code{list()}. Additional arguments passed
to \code{\link[=render_highquality]{render_highquality()}} when extracting the scene. Named arguments in \code{...}
that are only \code{\link[=render_highquality]{render_highquality()}} arguments are also passed to scene
extraction. \code{return_scene = TRUE} is always applied.}

\item{...}{Additional parameters to pass to \code{rayrender::render_animation()}.
Named arguments that are only \code{\link[=render_highquality]{render_highquality()}} arguments are passed to
scene extraction. If a name exists in both \code{\link[=render_highquality]{render_highquality()}} and
\code{rayrender::render_animation()}, it is passed to animation; use
\code{render_highquality_args} to pass overlapping names to scene extraction.}
}
\value{
Invisibly returns the value from \code{rayrender::render_animation()}.
}
\description{
Renders a high quality movie by extracting the current rayshader
scene with \code{\link[=render_highquality]{render_highquality()}}, reading saved rayrender interactive keyframes
with \code{rayrender::get_saved_keyframes()}, generating camera motion with
\code{rayrender::generate_camera_motion()}, and passing the result to
\code{rayrender::render_animation()}.
}
