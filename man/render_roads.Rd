% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_roads.R
\name{render_roads}
\alias{render_roads}
\title{Render Road Paths}
\usage{
render_roads(
  roads,
  roadcolor = "#303030",
  width = NULL,
  width_column = NULL,
  densify = TRUE,
  offset = 0,
  offset_transition = 0,
  layer = NULL,
  layer_height = 5.5,
  merge = TRUE,
  lane_texture = FALSE,
  lane_texture_file = NULL,
  lane_dash_length = 3,
  lane_gap_length = 10,
  lane_texture_length = NULL,
  lane_texture_mapping = c("auto", "fixed"),
  lanes = 2,
  lane_width = 3,
  lane_color = "#eeeade",
  centerline_color = "#eeeade",
  edge_line_color = "#d6ad3d",
  lane_line_width = 0.035,
  lane_dash_fraction = NULL,
  clear_previous = TRUE,
  preview = c("line", "mesh"),
  verbose = FALSE,
  parallel = TRUE,
  maximum_grade = 0.15,
  continuation_grade_tolerance = 0.14,
  zscale = 1,
  vertical_exaggeration = 1,
  heightmap = NULL
)
}
\arguments{
\item{roads}{Spatial line data used to draw road paths. Supports \code{sf},
\code{sfc}, \code{sfg}, \code{SpatialLines}, and \code{SpatialLinesDataFrame} line inputs.}

\item{roadcolor}{Default \code{"#303030"}. sRGB road surface color.}

\item{width}{Default \code{NULL}, which derives road width from \code{lanes} and
\code{lane_width}. If supplied, road width in scene grid-cell units for
\code{\link[=render_highquality]{render_highquality()}}. The rgl preview uses the same value as line width.}

\item{width_column}{Default \code{NULL}. Column name in an \code{sf} road object used
to set per-feature road widths. Values must be positive finite numbers and
use the same units as \code{width}. When supplied, road merging is disabled to
preserve feature attributes.}

\item{densify}{Default \code{TRUE}. Whether to densify road paths and resample
them along the terrain before \code{\link[=render_highquality]{render_highquality()}} meshing. Set to \code{FALSE}
to use the vertices returned by \code{\link[=render_path]{render_path()}} directly.}

\item{offset}{Default \code{0}. Additional vertical road base offset in elevation
units above the sampled surface.}

\item{offset_transition}{Default \code{0}. Length in scene/world units over which
each end of a road transitions between the sampled surface and \code{offset}.
Positive values use the terrain height at the path midpoint plus \code{offset} as
a constant deck height, with a quadratic Bezier ramp at each end. If a road
is shorter than twice this distance, each ramp is shortened to half the road
length.}

\item{layer}{Default \code{NULL}. An unquoted or character column name in an \code{sf}
road object containing OpenStreetMap-style layer values. Missing values are
treated as implicit layer \code{0}, except affirmative \code{bridge} or elevated
\code{location} metadata infer effective layer \code{1} and affirmative \code{tunnel} or
underground \code{location} metadata infer effective layer \code{-1}. Inferred values
remain distinguishable from explicit OSM layer tags in diagnostics.
Roads are grouped by exact physical events and conservative endpoint
continuations. A sparse quadratic profile solve enforces crossing clearance,
physical grade and grade-rate limits, junction height continuity, and
selected through-road grade compatibility. Surface roads retain a sampled
terrain reference, elevated spans use endpoint support chords, and roads with
explicit tunnel or underground metadata use a bounded terrain-relative
reference. Untagged negative layers are not independently activated as
tunnels. Ambiguous endpoint matches that would create a contradictory
clearance cycle are excluded. Supplying \code{layer} disables \code{merge} and requires
the suggested \code{sf}, \code{igraph}, \code{Matrix}, and \code{osqp} packages.}

\item{layer_height}{Default \code{5.5}. Either a single positive spacing in
elevation units between locally ordered layers, or an unquoted or character
column name containing each feature's positive separation above the lower
road at an intersection. Column heights override constant layer spacing.}

\item{merge}{Default \code{TRUE}. Whether to merge connected road linework before
rendering. This reduces visible joins between adjacent line features in
\code{\link[=render_highquality]{render_highquality()}}.}

\item{lane_texture}{Default \code{FALSE}. If \code{TRUE}, \code{\link[=render_highquality]{render_highquality()}} uses a
generated repeating texture on the top road surface for lane markings.}

\item{lane_texture_file}{Default \code{NULL}. Optional path to a custom road lane
texture. When supplied, it is used instead of the generated texture and
\code{lane_texture} is treated as \code{TRUE}.}

\item{lane_dash_length}{Default \code{3}. Painted dash length in scene/world units
for generated dashed lane markings. The default approximates a 10 foot dash
as 3 meters.}

\item{lane_gap_length}{Default \code{10}. Gap length in scene/world units for
generated dashed lane markings. The default approximates a 30 foot gap as
10 meters.}

\item{lane_texture_length}{Default \code{NULL}, which uses
\code{lane_dash_length + lane_gap_length}. Scene-unit length covered by one
repetition of the road lane texture.}

\item{lane_texture_mapping}{Default \code{"auto"}. If \code{"auto"}, each road path is
assigned a texture repetition count based on its world-unit length and
\code{lane_texture_length}. If \code{"fixed"}, all road paths use \code{lane_texture_length}
directly.}

\item{lanes}{Default \code{2}. Number of lanes used when generating the default
lane texture and, when \code{width = NULL}, deriving the road width. May also be
an unquoted or character column name in an \code{sf} road object. OSM-style
compound values use their first positive integer. Missing values first use
\code{lanes:forward} plus \code{lanes:backward}, then fall back to one lane for links
and one-way roads or two lanes for other roads.}

\item{lane_width}{Default \code{3}. Lane width in scene/world units used when
\code{width = NULL}. The derived road width is
\code{lane_width * (lanes + 2)}. The generated lane texture leaves one lane width
total outside the edge lines, split between both sides.}

\item{lane_color}{Default \code{"#eeeade"}. sRGB color for dashed lane divider
markings.}

\item{centerline_color}{Default \code{"#eeeade"}. sRGB color for the center
divider.}

\item{edge_line_color}{Default \code{"#d6ad3d"}. sRGB color for solid edge
markings.}

\item{lane_line_width}{Default \code{0.035}. Lane marking width as a fraction of
the road width in the generated texture.}

\item{lane_dash_fraction}{Default \code{NULL}, which uses
\code{lane_dash_length / (lane_dash_length + lane_gap_length)}. Fraction of each
texture repetition occupied by a dash for dashed lane markings.}

\item{clear_previous}{Default \code{TRUE}. If \code{TRUE}, removes the existing road
layer before drawing the new one. A clear-only call returns without rendering
a replacement.}

\item{preview}{Default \code{"line"}. Whether roads are drawn in rgl as lightweight
\code{"line"} previews or as their cached \code{"mesh"} geometry. The mesh is the exact
high-quality geometry and does not include the line-only preview offset.}

\item{verbose}{Default \code{FALSE}. Whether to display progress while building
and caching the road meshes.}

\item{parallel}{Default \code{TRUE}. Whether to use multiple native threads while
building the cached road meshes.}

\item{maximum_grade}{Default \code{0.15}. Maximum absolute longitudinal road
grade. Positive infinity removes the grade bound.}

\item{continuation_grade_tolerance}{Default \code{0.14}. Maximum absolute grade
mismatch at selected road continuations. Continuation height remains exact.}

\item{zscale}{Default \code{1}. The ratio between the x and y spacing and the z
axis. If omitted and \code{heightmap} is a spatial raster, rayshader uses the
raster cell resolution.}

\item{vertical_exaggeration}{Default \code{1}. Multiplier applied to the effective
visual relief. If omitted, rayshader uses the cached scene value from
\code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}} when available; pass explicitly to override for
this call.}

\item{heightmap}{Default \code{NULL}. Height matrix or spatial raster for the
current scene. If omitted, this is taken from the cached scene set by
\code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}}. Pass explicitly to override the cached value.}
}
\value{
Invisibly returns the rendered road coordinates. When \code{layer} is
supplied, the result has \code{terrain_following}, \code{profile_diagnostics}, and
\code{mesh_topology} attributes describing the sparse profile solve and selected
physical mesh continuations.
}
\description{
Adds road paths to the scene and eagerly builds their reusable
high-quality meshes. Roads can be previewed as rgl lines or as the same mesh
geometry consumed by \code{\link[=render_highquality]{render_highquality()}}. Road chains that cannot be
represented by either mesh method are omitted with a warning while the
remaining meshes continue rendering.
}
