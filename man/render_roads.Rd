% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_roads.R
\name{render_roads}
\alias{render_roads}
\title{Render Road Paths}
\usage{
render_roads(
  roads,
  heightmap = NULL,
  roadcolor = "#303030",
  zscale = 1,
  vertical_exaggeration = 1,
  width = NULL,
  width_column = NULL,
  densify = TRUE,
  offset = 0,
  merge = TRUE,
  lane_texture = FALSE,
  lane_texture_file = NULL,
  lane_dash_length = 3,
  lane_gap_length = 10,
  lane_texture_length = NULL,
  lane_texture_mapping = c("auto", "fixed"),
  lanes = 2,
  lane_width = 3,
  lane_color = "white",
  centerline_color = "#ffd23f",
  edge_line_color = "white",
  lane_line_width = 0.035,
  lane_dash_fraction = NULL,
  clear_previous = TRUE
)
}
\arguments{
\item{roads}{Spatial line data used to draw road paths. Supports \code{sf},
\code{sfc}, \code{sfg}, \code{SpatialLines}, and \code{SpatialLinesDataFrame} line inputs.}

\item{heightmap}{Default \code{NULL}. Height matrix or spatial raster for the
current scene. If omitted, this is taken from the cached scene set by
\code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}}. Pass explicitly to override the cached value.}

\item{roadcolor}{Default \code{"#303030"}. Road surface color.}

\item{zscale}{Default \code{1}. The ratio between the x and y spacing and the z
axis. If omitted and \code{heightmap} is a spatial raster, rayshader uses the
raster cell resolution.}

\item{vertical_exaggeration}{Default \code{1}. Multiplier applied to the effective
visual relief. If omitted, rayshader uses the cached scene value from
\code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}} when available; pass explicitly to override for this call.}

\item{width}{Default \code{NULL}, which derives road width from \code{lanes} and
\code{lane_width}. If supplied, road width in scene grid-cell units for
\code{\link[=render_highquality]{render_highquality()}}. The rgl preview uses the same value as line width.}

\item{width_column}{Default \code{NULL}. Column name in an \code{sf} road object used
to set per-feature road widths. Values must be positive finite numbers and use
the same units as \code{width}. When supplied, road merging is disabled to preserve
feature attributes.}

\item{densify}{Default \code{TRUE}. Whether to densify road paths and resample
them along the terrain before \code{\link[=render_highquality]{render_highquality()}} meshing. Set to \code{FALSE}
to use the vertices returned by \code{\link[=render_path]{render_path()}} directly.}

\item{offset}{Default \code{0}. Additional vertical road base offset in elevation
units above the sampled surface.}

\item{merge}{Default \code{TRUE}. Whether to merge connected road linework before
rendering. This reduces visible joins between adjacent line features in
\code{\link[=render_highquality]{render_highquality()}}.}

\item{lane_texture}{Default \code{FALSE}. If \code{TRUE}, \code{\link[=render_highquality]{render_highquality()}} uses a
generated repeating texture on the top road surface for lane markings.}

\item{lane_texture_file}{Default \code{NULL}. Optional path to a custom road lane
texture. When supplied, it is used instead of the generated texture and
\code{lane_texture} is treated as \code{TRUE}.}

\item{lane_dash_length}{Default \code{3}. Painted dash length in scene/world units
for generated dashed lane markings. The default approximates a 10 foot dash as
3 meters.}

\item{lane_gap_length}{Default \code{10}. Gap length in scene/world units for
generated dashed lane markings. The default approximates a 30 foot gap as
10 meters.}

\item{lane_texture_length}{Default \code{NULL}, which uses
\code{lane_dash_length + lane_gap_length}. Scene-unit length covered by one
repetition of the road lane texture.}

\item{lane_texture_mapping}{Default \code{"auto"}. If \code{"auto"}, each road path is
assigned a texture repetition count based on its world-unit length and
\code{lane_texture_length}. If \code{"fixed"}, all road paths use \code{lane_texture_length}
directly.}

\item{lanes}{Default \code{2}. Number of lanes used when generating the default
lane texture and, when \code{width = NULL}, deriving the road width.}

\item{lane_width}{Default \code{3}. Lane width in scene/world units used when
\code{width = NULL}. The derived road width is \code{lane_width * (lanes + 2)},
and the generated lane texture leaves one lane width total outside the edge
lines, split between both sides.}

\item{lane_color}{Default \code{"white"}. Color for dashed lane divider markings.}

\item{centerline_color}{Default \code{"#ffd23f"}. Color for the center divider.}

\item{edge_line_color}{Default \code{"white"}. Color for solid edge markings.}

\item{lane_line_width}{Default \code{0.035}. Lane marking width as a fraction of
the road width in the generated texture.}

\item{lane_dash_fraction}{Default \code{NULL}, which uses
\code{lane_dash_length / (lane_dash_length + lane_gap_length)}. Fraction of each
texture repetition occupied by a dash for dashed lane markings.}

\item{clear_previous}{Default \code{TRUE}. If \code{TRUE}, removes the existing road
layer before drawing the new one.}
}
\value{
Invisibly returns the rendered road coordinates.
}
\description{
Adds road paths to the scene. Roads are previewed as rgl lines
and rendered by \code{\link[=render_highquality]{render_highquality()}} as flat terrain-following rectangles.
}
