% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radiance_shade.R
\name{radiance_shade}
\alias{radiance_shade}
\title{Calculate Radiance Shade Overlay}
\usage{
radiance_shade(
  heightmap = NULL,
  texture = NULL,
  color = "grey50",
  zscale = 1,
  vertical_exaggeration = 1,
  filename = NA,
  samples = 128,
  sample_method = "sobol_blue",
  min_variance = 1e-07,
  shadow = TRUE,
  light = TRUE,
  lat = NA,
  long = NA,
  datetime = NA,
  sky_sun_elevation = NA,
  sky_sun_azimuth = NA,
  sky_altitude = NA,
  sky_args = list(),
  lightdirection = 315,
  lightaltitude = 45,
  lightsize = NULL,
  lightintensity = 500,
  lightcolor = "white",
  material = rayrender::diffuse(),
  water_attenuation = 0,
  water_surface_color = TRUE,
  water_ior = 1,
  override_material = FALSE,
  cache_scene = FALSE,
  reset_scene_cache = FALSE,
  width = NULL,
  height = NULL,
  clamp_value = NA,
  scene_elements = NULL,
  plot = FALSE,
  ...
)
}
\arguments{
\item{heightmap}{Default \code{NULL}. If \code{NULL}, uses the current rayshader rgl
scene when available, otherwise falls back to cached 2D hillshade metadata.
If a matrix (or supported raster input), renders a top-down radiance pass for
that data directly.}

\item{texture}{Default \code{NULL}. Texture used only when \code{heightmap} is supplied.
Can be a rayshader-style RGB(A) array, a built-in \code{\link[=sphere_shade]{sphere_shade()}} texture
name, a color vector (passed to \code{\link[=height_shade]{height_shade()}}), an image filename, or a
single color name.}

\item{color}{Default \code{"grey50"}. Fallback single-color texture when \code{heightmap}
is supplied and \code{texture = NULL}.}

\item{zscale}{Default \code{1}. Vertical scale when \code{heightmap} is supplied.}

\item{vertical_exaggeration}{Default \code{1}. Multiplier applied to the effective
visual relief. If omitted while rendering from the active scene, rayshader uses
the cached scene value from \code{\link[=plot_3d]{plot_3d()}} or \code{\link[=plot_gg]{plot_gg()}} when available; pass
explicitly to override for this call. This does not update cached metadata.}

\item{filename}{Default \code{NA}. Optional output filename. If supplied, the
render is also saved to disk.}

\item{samples}{Default \code{128}. Maximum samples per pixel.}

\item{sample_method}{Default \code{"sobol_blue"}, unless \code{samples > 256}, in which
case it is switched to \code{"sobol"}.}

\item{min_variance}{Default \code{1e-7}. Adaptive sampler variance threshold.}

\item{shadow}{Default \code{TRUE}. Whether to build the temporary top-down scene
with the rayshader shadow plane underneath the model. Setting this to \code{TRUE}
can help fill very dark corners in the radiance pass.}

\item{light}{Default \code{TRUE}. Whether to add directional lights.}

\item{lat}{Default \code{NA}. Latitude used for auto sky generation.}

\item{long}{Default \code{NA}. Longitude used for auto sky generation.}

\item{datetime}{Default \code{NA}. Datetime used for auto sky generation.}

\item{sky_sun_elevation}{Default \code{NA}. If supplied, uses
\code{skymodelr::generate_sky()} and passes this value to its \code{elevation} argument.}

\item{sky_sun_azimuth}{Default \code{NA}. If supplied, uses
\code{skymodelr::generate_sky()} and passes this value to its \code{azimuth} argument.}

\item{sky_altitude}{Default \code{NA}. If supplied, uses \code{skymodelr::generate_sky()}
and passes this value to its \code{altitude} argument.}

\item{sky_args}{Default \code{list()}. Extra arguments passed to
\code{skymodelr::generate_sky_latlong()} (default) or \code{skymodelr::generate_sky()}
when direct sky arguments are used.}

\item{lightdirection}{Default \code{315}. Light direction angle(s), in degrees.}

\item{lightaltitude}{Default \code{45}. Light altitude angle(s), in degrees.}

\item{lightsize}{Default \code{NULL}. Light radius; auto-derived from scene size.}

\item{lightintensity}{Default \code{500}. Light intensity value(s).}

\item{lightcolor}{Default \code{"white"}. Light color(s).}

\item{material}{Default \code{rayrender::diffuse()}. Forwarded to
\code{\link[=render_highquality]{render_highquality()}}.}

\item{water_attenuation}{Default \code{0}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{water_surface_color}{Default \code{TRUE}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{water_ior}{Default \code{1}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{override_material}{Default \code{FALSE}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{cache_scene}{Default \code{FALSE}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{reset_scene_cache}{Default \code{FALSE}. Forwarded to \code{\link[=render_highquality]{render_highquality()}}.}

\item{width}{Default \code{NULL}. Output width in pixels. If omitted, derived from
the underlying surface dimensions.}

\item{height}{Default \code{NULL}. Output height in pixels. If omitted, derived from
the underlying surface dimensions.}

\item{clamp_value}{Default \code{NA}. Radiance clamp value. Matches
\code{\link[=render_highquality]{render_highquality()}} default behavior.}

\item{scene_elements}{Default \code{NULL}. Extra rayrender objects to add.}

\item{plot}{Default \code{FALSE}. Whether to preview the rendered image.}

\item{...}{Additional parameters passed to \code{\link[=render_highquality]{render_highquality()}}.}
}
\value{
RGBA array representing the top-down radiance render.
}
\description{
Renders a top-down orthographic radiance pass of either the
current 3D rayshader scene or a supplied elevation matrix.

Cache fallback messages are disabled by default. Set \code{options(rayshader.verbose_scene_cache = TRUE)} to print when cached metadata is reused.

When called with no \code{heightmap}, this function uses the currently displayed
rayshader 3D scene when one exists. If no scene is open, it falls back to the
cached 2D hillshade heightmap and, when available, the cached hillshade/map
texture from the most recent rayshader hillshading chain. When a raw matrix is
supplied, rayshader builds a temporary surface and applies \code{texture} (or a
constant \code{color}, default \code{"grey50"}).

This function uses the same lighting controls as \code{\link[=render_highquality]{render_highquality()}},
including directional lights and auto-sky generation (\code{lat}, \code{long},
\code{datetime}, \code{sky_sun_elevation}, \code{sky_sun_azimuth}, \code{sky_altitude}, and
\code{sky_args}), and returns an RGBA array that can be passed to \code{\link[=add_overlay]{add_overlay()}}.
The render runs in an isolated background R session with
\code{RGL_USE_NULL=TRUE} in the subprocess environment, and uses the standard
rayshader pipeline:
\code{plot_3d(..., solid = TRUE, shadow = shadow)} followed by
\code{render_highquality(plot = FALSE)}.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
montereybay |>
height_shade(texture = topo.colors(256)) |>
add_overlay(radiance_shade(vertical_exaggeration = 10), 1) |>
plot_map()
#Plot using sky args
montereybay |>
height_shade(texture = topo.colors(256)) |>
add_overlay(radiance_shade(vertical_exaggeration = 10, 
                            sky_sun_elevation = 5, sky_sun_azimuth = 315), 1) |>
plot_map()
\dontshow{\}) # examplesIf}
}
