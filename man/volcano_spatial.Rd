% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano_spatial.R
\name{volcano_spatial}
\alias{volcano_spatial}
\title{Georeferenced Maungawhau Volcano Elevation Raster}
\source{
The base R \link[datasets:volcano]{datasets::volcano} data and the Geomorphometry.org
\href{https://www.geomorphometry.org/2009/08/20/volcano-maungawhau/}{"Volcano Maungawhau" reconstruction}.
}
\usage{
volcano_spatial()
}
\value{
A one-layer \code{terra::SpatRaster} containing elevation in meters.
}
\description{
Returns the base R \code{datasets::volcano} elevation model as a
georeferenced, one-layer \code{terra::SpatRaster}. Elevation is measured in meters,
and horizontal coordinates use EPSG:27200, NZGD49 / New Zealand Map Grid, on
a 10-meter grid.

The elevation data were digitized from a topographic map and should not be
treated as accurate survey data. A new independent copy of the raster is
returned on every call.
}
\examples{
volcano_dem = volcano_spatial()
terra::ext(volcano_dem)
terra::res(volcano_dem)
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
volcano_dem |>
  height_shade() |>
  add_shadow(ray_shade(),0) |>
  plot_3d()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=raster_to_matrix]{raster_to_matrix()}}
}
