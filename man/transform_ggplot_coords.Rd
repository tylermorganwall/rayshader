% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ggplot_extent.R
\name{transform_ggplot_coords}
\alias{transform_ggplot_coords}
\title{Transform Coordinates via ggplot2 Scales and Coord System}
\usage{
transform_ggplot_coords(x, y, panel = NULL, heightmap = NULL, crs = NULL)
}
\arguments{
\item{x}{Numeric (or discrete) x values in the original ggplot data space.}

\item{y}{Numeric (or discrete) y values in the original ggplot data space.}

\item{panel}{Default \code{NULL}. Facet panel index to use. Required if the scene has
multiple panels.}

\item{heightmap}{Default \code{NULL}. Height matrix returned by \code{\link[=plot_gg]{plot_gg()}} with
\code{save_height_matrix = TRUE}. If supplied, transformation metadata and extent are read
from this matrix instead of the active scene.}

\item{crs}{Default \code{NULL}. Only used for \code{coord_sf()}. Coordinate reference system for
input \code{x}/\code{y} values. Can be anything accepted by \code{\link[sf:st_crs]{sf::st_crs()}}. If \code{NULL}, values are
assumed to already be in the plot CRS.}
}
\value{
Data frame with columns \code{long} and \code{lat} in the same coordinate space as
\code{attr(result, "extent")}, suitable for \code{render_points()}/\code{render_path()}.
}
\description{
Transforms user coordinates through the same ggplot2 scale and coordinate
system used by the most recent \code{\link[=plot_gg]{plot_gg()}} scene.
}
\examples{
if(run_documentation()) {
library(ggplot2)

p = ggplot(mtcars) +
 geom_point(aes(wt, mpg, color = mpg)) +
 scale_x_log10()

plot_gg(p, width = 4)
pts = transform_ggplot_coords(x = c(2, 4), y = c(15, 30))

render_points(
 long = pts$long,
 lat = pts$lat,
 extent = attr(pts, "extent"),
 altitude = 20,
 color = "red",
 size = 5
)
render_snapshot()
}
}
